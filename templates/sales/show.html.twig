{% extends 'base.html.twig' %}

{% block title %}Sales{% endblock %}

{% block body %}
    <h1>Sales</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sale.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ sale.date ? sale.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Canceled</th>
                <td>{{ sale.canceled ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Painting title</th>
                <td>{{ sale.painting}}</td>
            </tr>
            <tr>
                <th>Customer</th>
                <td>{{ sale.customer.firstname}}
                    <br>{{ sale.customer.lastname}}
                    <br>{{ sale.customer.email}}
                    <br>{{ sale.customer.address}}
                    <br>{{ sale.customer.postalcode}}
                    <br>{{ sale.customer.city}}
                </td>
            </tr>
            <tr>
                <th>Painting price</th>
                <td>{{ sale.painting.price}}€</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sales_index') }}">back to list</a>

    <a href="{{ path('sales_edit', {'id': sale.id}) }}">edit</a>

    {{ include('sales/_delete_form.html.twig') }}
{% endblock %}
